#include <stdio.h>

/**
 * main - prints it's name
 * @argc: number of arguments
 * @argv: argument vector
 * Return: integer
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
